<odoo>

	<record id="project_task_time_wizard_form" model="ir.ui.view">
		<field name="name">Task Time wizard form</field>
		<field name="model">task.time.wizard</field>
		 <field name="inherit_id" ref="canet_maintenance.task_time_wizard_form"/>
		<field name="arch" type="xml">
			 <xpath expr="//field[@name='name']" position="attributes">
                    <attribute name="invisible">context.get('stop_process') or context.get('continue_process') or context.get('stop_task') or context.get('end_task')</attribute>
			 </xpath>
			 <xpath expr="//field[@name='description']" position="attributes">
                    <attribute name="invisible">context.get('start_process') or context.get('start_task') or context.get('continue_task') or context.get('restart_task')</attribute>
				 	<attribute name="required">context.get('stop_process') or context.get('continue_process') or context.get('stop_task') or context.get('end_task')</attribute>
			 </xpath>
		</field>
	</record>

</odoo>